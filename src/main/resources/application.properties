spring.application.name=speakflow-api

# Database Configuration (PostgreSQL)
spring.datasource.url=jdbc:postgresql://db.fqypazfgihnksiegkgfp.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=Rahul#818283
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# CORS Configuration
spring.mvc.cors.allowed-origins=http://localhost:5173,http://localhost:3000
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS

# OpenAI API Key
openai.api.key=${OPENAI_API_KEY:your_api_key_here}
